/// What a mess rule is about. Declaration order is the order categories are
/// shown in on the Rules screen. Persisted by name — don't rename values
/// without a migration.
enum RuleCategory { meals, bazar, payments, guests, cleanliness, quietHours, other }
